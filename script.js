// Aguarde o carregamento completo do documento antes de manipular o DOM
document.addEventListener('DOMContentLoaded', function () {
    const passwordInput = document.getElementById('password');

    // Remova as linhas relacionadas ao span e à função togglePasswordVisibility

    // Garanta que a senha esteja oculta inicialmente (opcional)
    passwordInput.type = 'password';
});
